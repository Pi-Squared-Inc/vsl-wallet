{
  "name": "vsl_snap_backend",
  "version": "0.1.5",
  "description": "The VSL MetaMask Snap that allows users to interact with the VSL blockchain.",
  "main": "./dist/bundle.js",
  "files": [
    "dist/",
    "snap.manifest.json"
  ],
  "scripts": {
    "allow-scripts": "npm run allow-scripts --workspace=root",
    "build": "mm-snap build",
    "build:clean": "yarn clean && yarn build",
    "clean": "rimraf dist",
    "prepublishOnly": "mm-snap manifest",
    "serve": "mm-snap serve",
    "start": "mm-snap watch",
    "test": "jest"
  },
  "dependencies": {
    "@metamask/keyring-api": "^17.5.0",
    "@metamask/keyring-snap-sdk": "^3.2.0",
    "@metamask/snaps-sdk": "~6.18.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.5.0",
    "@metamask/snaps-cli": "^6.6.0",
    "@metamask/snaps-jest": "^8.9.0",
    "@types/uuid": "^10.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Pi-Squared-Inc/vsl"
  },
  "engines": {
    "node": ">=18.6.0"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
